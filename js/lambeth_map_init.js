//INITIALISE... 
/*
jQuery(document).ready(function () {
    //Loop through all elements that instantiate a map
    lambeth_maps = [];
    jQuery('.lambeth_map').each(function (key, val) {
        lambeth_maps.push(new lambeth_map(val));
    });
});
*/

